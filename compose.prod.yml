services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "127.0.0.1:9000:9000"
    volumes:
      - flashcards_data:/app/data

volumes:
  flashcards_data: